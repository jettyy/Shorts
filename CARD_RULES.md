# 카드 대본 작성 규칙 (CARD_RULES.md)

> **이 파일은 클로드코드에게 주는 작업 지시서다.**
> 어떤 세션에서 작업하든, 원문을 카드 대본(`src/script.json`)으로 바꿀 때는
> 아래 기준을 그대로 따른다.

---

## 0. 대전제

- 원문 분석은 **API를 호출하지 않는다.** 이 저장소에는 API 키도, `.env`도, Anthropic 호출 코드도 없다.
- 원문을 읽고 카드로 재구성하는 일은 **그 세션의 클로드코드가 직접** 한다.
- 결과물은 `src/script.json` 파일 하나이고, 그 다음은 `npm run render` 한 줄이면 끝난다.

---

## 1. 카드 장수

- **6~8장**을 기본으로 한다. (원문이 짧으면 6장, 정보가 많으면 8장)
- 9장 이상으로 늘리지 않는다. 쇼츠는 40초 안에 끝나는 게 유리하다.
- 전체 길이는 **25~40초**를 목표로 한다.

---

## 2. 1번 카드 = 훅 (필수)

`"isHook": true`

첫 1.5초에 스크롤을 멈추게 하는 게 유일한 목적이다. 아래 중 하나를 쓴다.

| 유형 | 예시 |
| --- | --- |
| 궁금증 유발 질문 | `이 조건인데 왜\n한 푼도 못 받았을까` |
| 손실 경고 | `안 쓰는 카드 포인트\n1년 뒤엔 그냥 사라집니다` |
| 놀라운 숫자 | `버려지는 돈\n매년 1000억 원` |
| 통념 반박 | `연봉 높으면 못 받는다?\n사실이 아닙니다` |

**금지**: `오늘은 ~에 대해 알아보겠습니다`, `안녕하세요` 같은 도입부.
원문 제목을 그대로 베끼지 말고, 원문에서 **가장 의외였던 사실**을 훅으로 끌어올린다.

---

## 3. 마지막 카드 = CTA (필수)

`"isCta": true`

행동을 하나만 지정한다. 두 개 이상 시키면 아무것도 안 한다.

- 저장 유도: `지금 내 포인트부터\n확인해보세요`
- 팔로우 유도: `놓치는 혜택 시리즈\n계속 올립니다`
- 더보기 유도: `신청 방법은\n고정 댓글에`

---

## 4. 중간 카드 (2번 ~ 마지막-1번)

- **돈/혜택/결과와 직결되는 정보만** 남긴다. 배경 설명, 기관 소개, 제도 역사는 전부 버린다.
- 한 카드에 **정보는 하나**. 두 개 넣고 싶으면 카드를 나눈다.
- **한 문장 이내**로 압축한다. 접속사(`그리고`, `또한`, `하지만`)로 문장을 잇지 않는다.
- 숫자가 있으면 숫자를 앞세운다. `평균 2만 원`이 `꽤 많은 금액`보다 항상 강하다.
- 원문에 없는 수치나 사실을 **만들어내지 않는다.** 원문이 애매하면 애매한 채로 줄인다.
- 정보 배치 순서: **문제 → 규모 → 몰랐던 사실 → 해결 방법 → 걸리는 노력** 순이 잘 먹힌다.

---

## 5. title 표기 규칙

- **최대 3줄**, 한 줄은 **15자 내외**.
- 줄바꿈은 `\n`으로 **직접 지정한다.** 지정하지 않으면 자동 줄바꿈되지만,
  의미 단위로 끊는 편이 훨씬 읽기 좋다.
  - 좋음: `1인당 평균 2만 원이\n카드사에 묶여 있다`
  - 나쁨: `1인당 평균 2만 원이 카드사에\n묶여 있다`
- 마침표(`.`)는 쓰지 않는다. 물음표·느낌표는 훅에서만 아껴 쓴다.
- 폰트 크기는 글자 수에 맞춰 **자동으로 줄어든다**(`src/lib/text.ts`). 넘칠 걱정은 안 해도 되지만,
  글자가 많아질수록 작아지므로 **짧을수록 강하다**.

---

## 6. kicker (선택)

- 상단 골드 라벨. **8자 이내**.
- 그 카드를 한 단어로 분류하는 말만 쓴다: `몰라서 못 받는 돈`, `걸리는 시간`, `놓치면 손해`.
- 모든 카드에 다 붙이지 않는다. **2~3장에만** 붙여야 리듬이 생긴다.
- 훅 카드에 붙이면 주제를 빠르게 알려주는 효과가 있다.

---

## 7. durationSec (화면에 머무는 시간)

계산식:

```
durationSec = (공백 제외 글자 수 ÷ 4.5) + 0.8
→ 소수점 첫째 자리로 반올림
→ 최소 2.5초, 최대 5.0초로 자른다
```

- 기준 읽기 속도는 **초당 4~5글자**다.
- 훅 카드는 계산값에서 **+0.3초** 정도 여유를 줘도 좋다(첫 카드는 한 번 더 읽는다).
- CTA 카드는 계산값 그대로 둔다. 길게 끌면 이탈한다.
- `npm run check`가 이 계산을 자동으로 검증해준다.

---

## 8. script.json 형식

```json
{
  "topic": "파일명에 들어갈 짧은 주제",
  "source": "원문 출처 메모 (선택)",
  "cards": [
    {
      "kicker": "몰라서 못 받는 돈",
      "title": "안 쓰는 카드 포인트\n1년 뒤엔 그냥 사라집니다",
      "isHook": true,
      "durationSec": 4.6
    },
    {
      "title": "포인트 1점은 현금 1원\n계좌로 바로 받을 수 있다",
      "durationSec": 4.8
    },
    {
      "title": "지금 내 포인트부터\n확인해보세요",
      "isCta": true,
      "durationSec": 3.9
    }
  ]
}
```

| 필드 | 필수 | 설명 |
| --- | --- | --- |
| `index` | ✕ | 카드 번호. 생략하면 배열 순서대로 자동 부여 |
| `kicker` | ✕ | 상단 골드 라벨 (8자 이내) |
| `title` | ○ | 중앙 큰 텍스트. `\n`으로 줄바꿈, 최대 3줄 |
| `isHook` | ✕ | 첫 카드에만 `true` — 폰트가 더 커진다 |
| `isCta` | ✕ | 마지막 카드에만 `true` — 텍스트가 골드로 강조된다 |
| `durationSec` | ○ | 2.5 ~ 5.0 |

---

## 9. 작업 체크리스트 (클로드코드용)

원문을 받으면 이 순서로 진행한다.

1. 원문을 끝까지 읽고 **돈/혜택과 직결되는 팩트**만 목록으로 뽑는다.
2. 그중 **가장 의외인 것**을 훅(1번 카드)으로 올린다.
3. 나머지를 `문제 → 규모 → 몰랐던 사실 → 방법 → 노력` 순으로 4~6장에 배치한다.
4. 마지막에 CTA 카드를 붙인다.
5. 각 카드의 `durationSec`을 7번 계산식으로 배정한다.
6. `src/script.json`에 덮어쓴다.
7. `npm run check` → 에러가 나오면 고친다.
8. `npm run render` → `output/카드뉴스_[주제]_[타임스탬프].mp4` 생성.
9. 결과 파일 경로와 총 길이를 사용자에게 알려준다.

---

## 10. 디자인은 건드리지 않는다

카드 디자인(`src/components/Card.tsx`)은 **재사용 템플릿**이다.
새 주제를 만들 때는 `src/script.json`만 바꾼다.
톤을 바꿔야 할 때만 `src/theme.ts`의 색/타이포 토큰을 수정한다.
