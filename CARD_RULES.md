# 카드 대본 작성 규칙 (CARD_RULES.md)

> **이 파일은 클로드코드에게 주는 작업 지시서다.**
> 어떤 세션에서 작업하든, 원문을 `src/script.json` 으로 바꿀 때는 이 기준을 그대로 따른다.

---

## 0. 이 프로젝트가 만들려는 것

**기사 요약 자막 카드가 아니라, 직접 조사·해설한 정보 영상이다.**

유튜브는 "설명·해설·교육적 가치가 거의 없는 이미지 슬라이드쇼 또는 스크롤 텍스트"를
수익화 불가 사례로 명시한다. 개별 영상이 아니라 채널 전체를 보고 판단한다.
그래서 이 템플릿은 처음부터 아래를 전제로 설계돼 있다.

| 하지 않는 것 | 하는 것 |
| --- | --- |
| 원문 문장을 그대로 옮기기 | 사실만 가져와 **다시 쓰기** |
| 글자만 넘기기 | 카드마다 **도표·그래프** |
| 요약으로 끝내기 | **직접 계산·검증·주의점** 넣기 |
| 자막만 빠르게 넘기기 | **직접 녹음한 내레이션** 얹기 |
| 영상마다 똑같은 화면 | 주제마다 **테마·도표 종류 바꾸기** |

분석은 API를 호출하지 않는다. 그 세션의 클로드코드가 직접 읽고 판단한다.

---

## 1. 카드 구성 (권장 뼈대)

**7장**이 기본이다. 정보가 적으면 6장, 많으면 8장.

| # | type | 내용 | origin |
| --- | --- | --- | --- |
| 1 | `hook` | 구체적인 질문이나 사실 기반 훅 | 보통 `creator` |
| 2 | `fact` | 원문에서 확인한 핵심 사실 | `source` |
| 3 | `condition` | 누가 대상인지, 조건 정리 | `creator` |
| 4 | `example` | **직접 계산한 사례** ⭐ | `creator` |
| 5 | `fact` | 절차·방법 | `source` |
| 6 | `caveat` | **원문에 안 나오는 예외·주의점** ⭐ | `creator` |
| 7 | `conclusion` | 제작자의 결론 + 짧은 CTA | `creator` |

⭐ 표시한 두 장이 이 영상을 "요약본"이 아니게 만드는 핵심이다. **빼지 않는다.**

- `origin: "creator"` 카드가 **3장 이상**이어야 한다.
- 마지막 카드를 CTA로만 채우지 않는다. 결론을 말하고 **CTA는 `cta` 필드에 한 줄**로 붙인다.

---

## 2. 카드마다 도표를 붙인다

`visual` 필드에 도표를 넣는다. 종류와 데이터 형식은 **[docs/VISUALS.md](./docs/VISUALS.md)** 참고.

- 글자만 있는 카드는 **1장 이하**로 한다.
- 도표 종류를 한 영상 안에서 섞는다. 7장이 전부 `checklist` 면 의미가 없다.
- 억지로 차트를 만들지 않는다. 숫자 하나가 핵심이면 `stat`, 절차면 `flow` 가 맞다.
- **항목 수 제한은 없다.** 도표는 항목 수에 맞춰 글자·행 높이가 자동으로 줄어든다
  (`src/lib/density.ts`). 다만 읽을 수 있는 글자 크기(26px)에 닿으면 더 못 줄인다.
  넘치면 **항목을 지우지 말고 카드를 나눠서 이어 보여준다.** `npm run check` 가 몇 장으로
  나눌지 알려준다.
- 한 카드 최대: ranklist 14행 / bar 8개 / checklist 10개 / table 10행 / calc 12줄 /
  timeline 8개 / flow(세로) 7개. 글자가 두 줄로 접히는 항목은 이 절반으로 잡는다.

| 카드 역할 | 잘 맞는 도표 |
| --- | --- |
| `hook` | `stat` (놀라운 숫자 하나) |
| `fact` | `table`, `flow`, `trend` |
| `condition` | `checklist` |
| `example` | `calc`, `bar` |
| `caveat` | `checklist` (`ok: false` 위주) |
| `conclusion` | `timeline`, `stat` |

---

## 3. 원문 문장을 그대로 쓰지 않는다

- 사실과 숫자는 가져오되, **문장은 처음부터 다시 쓴다.**
- 원문에 없는 수치나 사실을 **만들어내지 않는다.** 원문이 애매하면 애매한 채로 줄인다.
- 원문을 `.source/current.txt` 에 저장해두면 `npm run check` 가
  연속 14자 이상 겹치는 문장을 자동으로 잡아준다. (이 폴더는 git에 올라가지 않는다)
- 출처는 `source.url` 과 `source.checkedOn`(확인 기준일)에 **반드시** 적는다.
  마지막 카드 하단에 자동으로 표기된다.

---

## 4. title 표기 규칙

- **최대 3줄**, 한 줄 15~17자.
- 줄바꿈은 `\n` 으로 직접 지정한다. 의미 단위로 끊는다.
  - 좋음: `카드 5장 쓰는 사람이\n1년에 버리는 돈`
  - 나쁨: `카드 5장 쓰는 사람이 1년에\n버리는 돈`
- 마침표는 쓰지 않는다. 물음표·느낌표는 훅에서만.
- **도표가 있는 카드는 제목이 자동으로 작아진다.** 주인공은 도표다.
  제목은 도표가 무슨 말을 하는지 알려주는 한 줄이면 충분하다.
- `body` 에 보조 설명 한 줄을 넣을 수 있다(선택).

### 1번 카드는 곧 썸네일이다

**영상의 0프레임이 1번 카드가 완성된 화면**이다.
유튜브·인스타가 첫 프레임을 커버로 잡아가기 때문에, 1번 카드는 등장 연출 없이
처음부터 글자와 도표가 다 보이도록 렌더링된다(`Card.tsx` 의 `isFirst`).

그래서 1번 카드는 **그 한 장만 봐도 내용이 전달돼야 한다.**
- 제목만으로 무슨 영상인지 알 수 있게 쓴다
- `stat` 처럼 숫자 하나가 크게 박히는 도표가 썸네일로 가장 강하다
- 훅 카드에 `kicker` 를 붙이면 주제가 한눈에 들어온다

### 1번 카드(훅) 쓰는 법

| 유형 | 예시 |
| --- | --- |
| 직접 계산한 숫자 | `카드 5장 쓰는 사람이\n1년에 버리는 돈` |
| 통념 반박 | `연봉 높으면 못 받는다?\n사실이 아닙니다` |
| 직접 해봤다 | `제 카드로 실제\n돌려봤습니다` |
| 조건 제시 | `이 조건이면\n안 받으면 손해입니다` |

**금지**: `오늘은 ~에 대해 알아보겠습니다`, `안녕하세요`, 원문 제목 그대로 쓰기.

---

## 5. narration (전 카드 필수)

카드마다 **직접 읽을 문장**을 쓴다. 자막을 그대로 읽는 게 아니라, 화면에 없는 설명을 더한다.

```
title:     "제 카드로 실제 돌려봤습니다"
narration: "말로만 하면 감이 안 오니까 제 카드로 실제 돌려봤습니다."
```

- 한 카드에 **한두 문장**. 길면 카드가 늘어진다.
- 말투는 구어체로. 읽었을 때 어색하면 다시 쓴다.
- `npm run narration` → 녹음용 대본(`output/내레이션_*.md`)이 나온다.
- `npm run narration -- --fit` → 읽는 속도에 맞춰 카드 길이를 다시 계산한다.

---

## 6. durationSec

**내레이션이 있으므로 읽는 시간이 기준이다.**

```
durationSec = (내레이션 공백 제외 글자 수 ÷ 5.2) + 0.7초
→ 최소 2.5초
```

직접 계산할 필요는 없다. `npm run narration -- --fit` 이 전부 맞춰준다.
전체 길이는 **35~55초**를 목표로 한다.

---

## 7. accent (영상 테마)

주제가 바뀌면 `accent` 도 바꾼다. 모든 영상이 같은 색이면 한 영상처럼 보인다.
`gold` / `mint` / `coral` / `violet` / `ice` — 고르는 기준은 [docs/VISUALS.md](./docs/VISUALS.md).

---

## 8. script.json 형식

```json
{
  "topic": "파일명에 들어갈 짧은 주제",
  "accent": "gold",
  "source": {
    "title": "원문 제목",
    "publisher": "매체명",
    "url": "https://...",
    "checkedOn": "2026-09-19"
  },
  "narrationAudio": "audio/녹음파일.mp3",
  "cards": [
    {
      "type": "hook",
      "origin": "creator",
      "kicker": "직접 계산해봤다",
      "title": "카드 5장 쓰는 사람이\n1년에 버리는 돈",
      "narration": "카드를 다섯 장 쓰는 사람이 1년에 얼마를 버리는지 직접 계산해봤습니다.",
      "durationSec": 6.1,
      "visual": {
        "kind": "stat",
        "value": "23,000",
        "unit": "원",
        "caption": "카드사별 평균 잔여 포인트 × 5장 기준, 직접 산출"
      }
    }
  ]
}
```

| 필드 | 필수 | 설명 |
| --- | --- | --- |
| `type` | ○ | hook / fact / condition / example / caveat / conclusion |
| `origin` | ○ | `source`(원문 확인) 또는 `creator`(직접 분석) |
| `title` | ○ | 최대 3줄 |
| `narration` | ○ | 직접 읽을 문장 |
| `durationSec` | ○ | `--fit` 이 자동 계산 |
| `kicker` | ✕ | 상단 라벨(8자 이내). 없으면 type 기본 라벨 |
| `body` | ✕ | 제목 아래 보조 설명 한 줄 |
| `visual` | ✕ | 도표 — [docs/VISUALS.md](./docs/VISUALS.md) |
| `cta` | ✕ | 마지막 카드에만. 결론 아래 한 줄 |

---

## 9. 작업 순서 (클로드코드용 체크리스트)

원문을 받으면 이 순서로 진행한다.

1. 원문을 `.source/current.txt` 에 저장한다 (베낀 문장 자동 검사용).
2. 원문에서 **사실과 숫자만** 목록으로 뽑는다. 문장은 가져오지 않는다.
3. 뽑은 사실로 **직접 계산할 수 있는 게 뭔지** 찾는다 → `example` 카드.
4. 원문이 빠뜨린 조건·예외를 찾는다 → `caveat` 카드. (모르면 일반적으로 알려진 주의점)
5. 1번 규칙의 뼈대대로 7장을 배치하고, 각 카드에 맞는 `visual` 을 고른다.
6. 카드마다 `narration` 을 쓴다.
7. `src/script.json` 저장 → `npm run narration -- --fit` (길이 자동 조정)
8. `npm run check` → ERROR가 있으면 고친다. 💡 안내는 판단해서 반영한다.
9. `npm run render` → `output/카드뉴스_[주제]_[타임스탬프].mp4`
10. 사용자에게 알린다: **결과 파일 경로, 총 길이, 그리고 녹음 대본 경로**
    (`output/내레이션_*.md` — 목소리를 얹으라고 안내한다).

---

## 10. 디자인은 건드리지 않는다

`src/components/` 는 재사용 템플릿이다. 새 주제를 만들 때는 `src/script.json` 만 바꾼다.
톤을 바꿔야 하면 `accent` 를 바꾸고, 그래도 안 되면 `src/theme.ts` 토큰을 수정한다.
