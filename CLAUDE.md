# 이 저장소에서 작업할 때 (클로드코드용 지침)

## 이 프로젝트가 하는 일

기사·블로그 원문을 **도표가 들어간 해설형 세로 쇼츠**(1080×1920 mp4)로 만드는 Remotion 프로젝트다.

## 가장 중요한 두 가지

**1. 원문 → 대본 변환은 API를 호출하지 않는다. 클로드코드가 직접 한다.**
이 저장소에 Anthropic API 호출 코드, API 키, `.env`, `dotenv`를 **추가하지 않는다.**

**2. 결과물은 "기사 요약 자막 카드"가 되면 안 된다.**
유튜브가 수익화 불가로 명시한 "해설·교육적 가치가 거의 없는 이미지 슬라이드쇼"에
해당하지 않도록, 아래를 반드시 지킨다.
- 원문 문장을 그대로 옮기지 않는다. 사실과 숫자만 가져와 다시 쓴다.
- 카드마다 도표(`visual`)를 붙인다. 글자만 있는 카드는 1장 이하.
- **1번 카드에는 `headline`(주제 배너)을 반드시 넣는다.** 첫 화면에서 "이게 무슨
  영상인지" 가 안 보이면 그냥 넘어간다. 첫 프레임은 썸네일로도 잡히는 자리다.
  주제 + 범위가 다 들어간 8~20자 한 줄 — "한국 부자 TOP 20", "공기업 평균연봉 TOP 16".
  1번 카드의 `title` 은 배너를 되풀이하지 말고 **후킹 문장**(가장 센 숫자·격차·오해)을 쓴다.
- **원문의 핵심이 목록·순위·표 자체면 전부 보여준다.** 상위 몇 개만 뽑고 나머지를
  설명으로 때우면 핵심이 빠진 지루한 영상이 된다. 항목이 카드에 안 들어가면
  **항목을 지우는 게 아니라 카드를 나눈다**(`ranklist` + `rank` 직접 지정).
- 주제에 맞는 구성을 고른다 — ranking / explainer / howto / compare / change.
  모든 원문을 같은 7장 뼈대에 밀어 넣지 않는다 (`app/prompt.mjs` 1단계 참고).
- **직접 계산(`type: "example"`)과 주의점(`type: "caveat"`) 카드를 반드시 넣는다.**
- 카드마다 `narration`을 쓰고, 녹음 대본을 만들어 사용자에게 안내한다.
- 주제가 바뀌면 `accent` 테마와 도표 종류를 바꾼다.

## 두 가지 사용 방식

1. **브라우저 앱** (`npm start` → http://localhost:4321) — 원문 입력·대본 확인·녹음·출력을
   화면에서 처리한다. 앱은 대본 생성을 위해 `claude -p` 를 호출한다(app/server.mjs).
2. **클로드코드에서 직접** — 아래 순서대로 한다.

## 원문을 받았을 때 할 일

1. **[CARD_RULES.md](./CARD_RULES.md) 와 [docs/VISUALS.md](./docs/VISUALS.md) 를 읽는다.**
2. 원문을 `.source/current.txt` 에 저장한다 (베낀 문장 자동 검사용, git 제외).
3. 규칙대로 `src/script.json` 을 덮어쓴다.
4. `npm run narration -- --fit` — 내레이션 길이에 맞춰 카드 길이 자동 조정.
5. `npm run check` — ERROR는 고치고, 💡 안내는 판단해서 반영.
6. `npm run render` — `output/카드뉴스_[주제]_[타임스탬프].mp4`
7. 사용자에게 **영상 경로 + 총 길이 + 녹음 대본 경로**를 알리고,
   목소리를 얹는 방법(README의 "내레이션 녹음해서 얹기")을 안내한다.

## 앱(app/) 관련 메모

- `app/server.mjs` 는 의존성 없이 node 기본 모듈만 쓴다. 패키지를 추가하지 않는다.
- ffmpeg/ffprobe 는 `node_modules/@remotion/compositor-*/` 의 바이너리를 직접 호출한다.
  (`npx remotion ffmpeg` 는 호출당 1.4초가 들어서 쓰지 않는다. 못 찾으면 npx 로 폴백)
- 녹음 길이와 음량 측정값은 `app/data/clips/meta.json` 에 캐시된다
  (예전 형식인 숫자만 있는 항목도 읽을 수 있어야 한다).
- **미디어 파일은 Range(부분 요청)를 지원해서 내려줘야 한다.** `serveFile` 이 처리한다.
  사파리는 `<audio>`/`<video>` 재생 전에 Range 요청을 보내고 206 을 기대한다.
  200 으로 통째로 주면 재생이 "오류"로 죽는다(크롬은 넘어가서 안 보인다).
  Content-Length 도 반드시 같이 준다.
- **녹음 형식을 특정 형식으로 강제하지 않는다.** 크롬은 webm(opus), **사파리는 mp4(AAC)** 다.
  `pickRecordOptions()` 가 `isTypeSupported` 로 되는 걸 골라 쓰고, 하나도 없으면
  옵션 없이 브라우저에 맡긴다. 서버는 ffmpeg 로 wav 변환하므로 어떤 형식이 와도 된다.
  원본은 `card-N.rec` 로 저장한다 — 확장자로 형식을 표현하면 지울 때 빠뜨리게 된다.
- **녹음할 때 `autoGainControl` 을 켜지 않는다.** 켜면 녹음 중에 볼륨이 계속 조절돼서
  말 시작은 크고 뒤로 갈수록 작아진다. 카드마다 반복되어 소리가 출렁인다.
  음량은 합칠 때 loudnorm 2패스(-16 LUFS)로 카드별로 맞춘다.
  1패스로 재고 그 값으로 2패스를 돌려야 선형 보정이라 음량이 출렁이지 않는다.
- 렌더 직전에 항상 내레이션을 다시 합치므로(`buildAudio`), 녹음과 영상이 어긋나지 않는다.
- `buildAudio` 는 **여러 번 돌려도 길이가 누적되면 안 된다.** 그래서 길이를 항상
  변하지 않는 값에서 다시 계산한다 — 녹음이 있으면 녹음 파일의 실제 길이,
  없으면 내레이션 글자 수(`npm run narration --fit` 과 같은 기준).
  `script.durationSec` 을 입력으로 삼지 않는다.
- 재생 속도(`app/data/render-settings.json`)는 소리(atempo)와 카드 길이에 같이 적용한다.
  카드 사이 여백은 속도를 적용한 **뒤에** 붙인다. 길이는 0.1초 단위로 맞춘다
  (30fps에서 0.1초 = 정확히 3프레임이라 영상과 소리가 어긋나지 않는다).
- **2배속은 넣지 않는다.** 도표를 읽을 시간이 사라지고 목소리가 부자연스러워져
  이 프로젝트가 피하려는 "양산형" 인상을 준다. 허용 값은 1 / 1.1 / 1.25 뿐이다.
- `app/data/` 는 git 제외 대상이다.
- **한 작업이 끝나고 다음 작업을 시작하면 이전 영상의 흔적이 남으면 안 된다.**
  4단계 출력 화면은 그릴 때마다 먼저 비우고(`clearOutputUI`), `GET /api/videos` 의
  `current`(= 영상이 `src/script.json` 보다 나중에 만들어졌나)가 참일 때만 올리기 영역을 연다.
  `POST /api/reset` 은 대본·원문·녹음·합친 내레이션을 지운다.
  `src/script.json` 은 `src/Root.tsx` 가 정적으로 import 하므로 **파일을 지우지 않고**
  카드가 빈 대본으로 되돌린다. 만들어 둔 영상(`output/`)은 지우지 않는다.
- JSON 응답에는 `Cache-Control: no-store` 를 붙인다. 안 붙이면 대본을 새로 만들어도
  브라우저가 이전 문구 응답을 재사용한다.

## 업로드 관련 메모

- `app/publish-copy.mjs` 가 플랫폼별 문구를 만든다. 대본에 `publish` 블록이 있으면
  그걸 쓰고, 없는 항목만 대본 내용으로 채운다(추가 API 호출 없음).
- 만드는 문구는 **유튜브·인스타·쓰레드 세 가지뿐이다.** 페이스북은 쓰지 않는다.
- 출력 파일명은 **`날짜시간_제목`** 순서다(예: `20260920-054631_카드포인트현금화.mp4`).
  만든 순서대로 정렬되게 하려는 것이니 순서를 바꾸지 않는다.
- 렌더가 끝나면 영상과 문구(txt)를 **자동으로 내려받게 한다**(`autoSave`).
  문구는 `/api/publish/copy.txt` 가 만들어 주며, 윈도우 메모장 호환을 위해
  BOM 을 붙이고 줄바꿈을 CRLF 로 맞춘다.
- 파일 저장 헤더는 `attachment()` 로 만든다. 한글 이름은 `filename*=UTF-8''` 로 보내되,
  이것만 있으면 못 읽는 클라이언트가 있어 ASCII 대체 이름도 `filename=` 에 같이 넣는다.
- 인스타 해시태그는 **정확히 5개**로 맞춘다.
- **업로드 문구에는 확인 기준일(날짜)을 넣지 않는다.** 날짜는 영상 마지막 카드에만 둔다.
  문구에 날짜가 박히면 나중에 다시 올릴 때 문구를 고쳐야 한다.
  1단계에서 입력받지 않고 대본 만든 날이 자동으로 들어가며,
  `publish-copy.mjs` 의 `stripCheckedOn` 이 예전 대본의 `publish` 블록에 남은 날짜도 걷어낸다.
- **쓰레드만 말투가 다르다.** 반말에 이모지 한두 개를 넣어 친근하게 쓴다.
  존댓말과 섞지 않는다. 해시태그는 쓰레드가 한 개만 지원하므로 1개 이하로 붙인다.
- `app/youtube.mjs` 는 외부 패키지 없이 fetch 로 OAuth + resumable upload 를 한다.
  googleapis 같은 패키지를 추가하지 않는다.
- **업로드가 끊겨도 처음부터 다시 올리지 않는다.** resumable 업로드는 본문이 다
  도착한 뒤에도 응답을 못 받는 일이 있는데, 그때 다시 올리면 새 세션이 열려서
  **같은 영상이 두 번 올라간다.** 실제로 사용자 채널에 똑같은 영상이 두 개 생겼다
  (할당량도 3200 units 나갔다). 끊기면 `askSessionResult()` 가 **같은 세션에**
  본문 없이 `Content-Range: bytes */크기` 로 물어본다 — 다 받았으면 유튜브가
  최종 응답(영상 정보)을 그대로 주므로 성공으로 처리하고, 308 이면 실패로 알린다.
  `npm run test:upload` 가 "세션을 한 번만 연다"를 지킨다.
- **OAuth 권한을 늘리지 않는다.** 늘리면 이미 연결해둔 계정이 전부 다시 로그인해야 한다.
  `youtube.upload` + `youtube.readonly` 두 개로 끝낸다.
- OAuth 클라이언트와 토큰은 `app/data/youtube.json` 에만 저장한다(git 제외).
- **업로드 본문 스트림에 `on('data')` 리스너를 붙이지 않는다.** 붙이는 순간 스트림이
  flowing 모드가 되어 fetch(undici)가 읽기 전에 조각이 새어나간다. 보낸 양이
  Content-Length 보다 적어지면 요청이 끊기고, 화면에는 원인을 알 수 없는 "fetch failed"
  만 뜬다(UND_ERR_REQ_CONTENT_LENGTH_MISMATCH). 진행률은 비동기 제너레이터로
  **yield 하면서 센다** — 실제 전송량과 정확히 같아진다. `npm run test:upload` 가 지킨다.
- **fetch 실패는 `netFail()` 로 감싼다.** Node 의 fetch 는 무슨 일이 있어도 메시지가
  "fetch failed" 한 줄이고 진짜 원인은 `error.cause` 에 있다. 꺼내서 보여주지 않으면
  아무 단서도 안 남는다. 원인이 여러 겹일 때가 있으니(`fetch failed` → `SocketError`
  → `ECONNRESET`) **겹을 끝까지 따라가서** 다 보여준다.
- **유튜브 오류는 `explain()` 을 통과시켜 "무엇을 하라"로 바꿔서 보여준다.**
  구글은 영어 JSON 을 그대로 준다. 특히 `accessNotConfigured`(프로젝트에 YouTube Data
  API v3 미사용)는 **공개 설정과 무관하게 모든 업로드를 막는데**, 생짜로 보여주면
  사용자가 비공개 문제로 오해한다. 오류에 들어 있는 프로젝트 번호로 사용 설정 링크를
  만들어 준다. 화면에서는 링크를 눌러서 갈 수 있게 렌더한다.
- **처음 연결할 때 가장 많이 막히는 건 "테스트 사용자" 미등록이다.** 구글 클라우드의
  OAuth 앱이 "테스트" 상태면 등록된 테스터만 로그인할 수 있고, 아니면 403 access_denied 가 난다.
  UI 의 클라이언트 등록 안내와 콜백 오류 화면에 이 사실을 적어둔다. 이 안내를 빼지 않는다.
  또 "테스트" 상태에서 받은 토큰은 **7일 뒤 만료된다**(구글 정책) — 예약 발행이 깨지므로
  프로덕션으로 게시하라고 안내한다.
- **업로드 방식 기본값은 "비공개로 올리기"다** (사용자 요청). 선택지 순서는
  비공개로 올리기 / 예약 발행 / 바로 공개다. 예약·공개는 심사(감사) 전에는
  유튜브가 막을 수 있어서, 확실히 되는 비공개를 기본으로 둔다.
- 유튜브는 미심사 프로젝트로 올리면 영상이 강제 비공개가 되고 **예약 시각이 와도
  공개되지 않을 수 있다.** 이 사실을 화면에 명시한다. 이 경고를 빼지 않는다.
- **예약 시각은 자동으로 추천한다.** 채널에 이미 예약된 영상 중 가장 늦은 공개 예정
  시각을 찾아 **그 뒤 3~5시간 사이**를 랜덤으로 고른다(5분 단위). 한 시간대에 몰리지
  않게 하려는 것이다. 예약이 없거나 이미 지났으면 "지금"을 기준으로 잡는다.
  5분 단위로 맞추느라 반올림이 **양쪽으로** 넘칠 수 있다 — 내려가면 3시간보다 짧아지고
  올라가면 5시간을 넘는다. 넘은 쪽으로만 5분을 옮겨 항상 3~5시간 안에 둔다.
  이걸 위해 OAuth 에 `youtube.readonly` 를 같이 요청한다 — 권한이 없어도(예전에 연결한
  계정) 업로드는 되고 추천만 "지금 기준"으로 바뀐다. 추천 실패가 예약을 막으면 안 된다.
- 인스타·쓰레드는 공개 URL 호스팅이 필요하고 네이티브 예약이 없어서 아직 문구만 만든다.
- **배경음악 기능은 넣지 않는다.** 한 번 만들었다가 사용자 요청으로 걷어냈다.

## 건드리지 말 것

- **영상의 0프레임은 1번 카드가 완성된 화면이어야 한다.** 유튜브·인스타가 첫 프레임을
  커버(썸네일)로 잡아간다. `Card.tsx` 의 `isFirst` 와 `visuals/useReveal.ts` 의
  `InstantContext` 가 이걸 처리한다. 1번 카드에 등장 연출을 되살리지 않는다.
- `src/components/` 의 디자인은 재사용 템플릿이다. 새 주제를 만들 때는 손대지 않는다.
- 도표 색은 색각이상·대비 검증을 거쳤다(`src/components/chartTheme.ts`).
  "하나만 강조, 나머지는 회색" 원칙을 깨지 않는다.
- **테마(`accent`)는 색만 바꾸는 게 아니다.** 배경 그라데이션 네 단계 전부·방향·
  글로우 위치·배경 질감이 테마마다 다르다(`src/theme.ts`, `components/Background.tsx`).
  포인트 색 하나만 바꾸면 화면의 대부분인 배경이 똑같아서 다섯 테마가 전부 같아 보인다.
  새 테마의 `bg` 중간 단계는 `#122350` 보다 밝게 잡지 않는다(도표 대비가 깨진다).
- 새 도표 종류를 추가할 때만 `src/components/visuals/` 에 파일을 만들고
  `types.ts` 의 `Visual` 유니온과 `visuals/index.tsx` 분기에 등록한다.
- **도표에 고정 크기를 다시 넣지 않는다.** 항목 수에 맞춰 `lib/density.ts` 의
  `fitDensity` 로 글자·여백을 줄인다. 읽을 수 있는 하한은 26px(`MIN_FONT`)이고,
  거기 닿으면 카드를 나누라고 `npm run check` 가 알려준다.
  컴포넌트의 설계 원본 크기를 바꾸면 `scripts/check-script.mjs` 의
  `VISUAL_SHAPE` 표도 같이 고쳐야 한다(두 곳의 숫자가 같아야 경고가 맞는다).

## 환경 메모

- 렌더링에 Chrome/Chromium이 필요하다. 자동 다운로드가 막힌 환경에서는
  `scripts/find-browser.mjs` 가 설치된 브라우저를 찾아 쓴다.
  직접 지정하려면 `REMOTION_BROWSER_EXECUTABLE` 환경변수.
- 한글 폰트는 `npm install` 시 node_modules에서 `public/fonts/` 로 복사된다. 외부 CDN 미사용.
